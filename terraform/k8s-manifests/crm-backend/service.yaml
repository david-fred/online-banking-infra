apiVersion: v1
kind: Service
metadata:
  name: crm-backend-service
  namespace: default
spec:
  selector:
    app: crm-backend
  ports:
    - protocol: TCP
      port: 5000
      targetPort: 5000
  type: ClusterIP